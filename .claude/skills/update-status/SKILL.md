---
name: update-status
description: Update CLAUDE.md's "Current State" section to reflect what was actually built or changed in this session. Use after finishing a feature or notable change in the RiftDesktop project, or when the user runs /update-status.
---

Review what was built or changed in this session, then update the "CURRENT
STATE" section of CLAUDE.md to accurately reflect it — what's actually working,
what's stubbed/placeholder, what doesn't exist yet, screen by screen. Keep the
rest of CLAUDE.md unchanged unless something in this session invalidated
earlier decisions (e.g. a data model field changed) — if so, update that
section too and flag what changed.
